_schema-version: "3.2"
ID: hmelpaper
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: hmelpaper-destination-content
  type: com.sap.application.content
  requires:
  - name: hmelpaper-destination-service
    parameters:
      content-target: true
  - name: hmelpaper-repo-host
    parameters:
      service-key:
        name: hmelpaper-repo-host-key
  - name: hmelpaper-uaa
    parameters:
      service-key:
        name: hmelpaper-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: hmelpaper_html_repo_host
          ServiceInstanceName: hmelpaper-html5-service
          ServiceKeyName: hmelpaper-repo-host-key
          sap.cloud.service: hmelpaper
        - Authentication: OAuth2UserTokenExchange
          Name: hmelpaper_uaa
          ServiceInstanceName: hmelpaper-xsuaa-service
          ServiceKeyName: hmelpaper-uaa-key
          sap.cloud.service: hmelpaper
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: hmelpaper-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: hmelpaper-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - hmelpaper.zip
      name: hmelpaper
      target-path: resources/
- name: hmelpaper
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: hmelpaper-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: hmelpaper-destination-service
    service-plan: lite
- name: hmelpaper-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: hmelpaper-xsuaa-service
    service-plan: application
- name: hmelpaper-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: hmelpaper-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
