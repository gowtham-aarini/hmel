_schema-version: "3.2"
ID: operations
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: operations-destination-content
  type: com.sap.application.content
  requires:
  - name: operations-destination-service
    parameters:
      content-target: true
  - name: operations-repo-host
    parameters:
      service-key:
        name: operations-repo-host-key
  - name: operations-uaa
    parameters:
      service-key:
        name: operations-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: operations_html_repo_host
          ServiceInstanceName: operations-html5-service
          ServiceKeyName: operations-repo-host-key
          sap.cloud.service: operations
        - Authentication: OAuth2UserTokenExchange
          Name: operations_uaa
          ServiceInstanceName: operations-xsuaa-service
          ServiceKeyName: operations-uaa-key
          sap.cloud.service: operations
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: operations-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: operations-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - operations.zip
      name: operations
      target-path: resources/
- name: operations
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: operations-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: operations-destination-service
    service-plan: lite
- name: operations-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: operations-xsuaa-service
    service-plan: application
- name: operations-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: operations-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
